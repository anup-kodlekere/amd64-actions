name: Sample Workflow

on: [ workflow_dispatch, push ]


jobs:
  power_job:
    name: Power Job
    runs-on: ubuntu-latest
    steps:
      - name: Run commands
        run: |
          docker version
